package com.dragonboatrace.tools.state;

/**
 * StateAdapter is responsible for serialising / deserialising the state.
 * <p>
 * Note: the state file is a JSON file with a top-level array.
 * Each item is a JSON object that consists of the save slot, or is an empty JSON object if the slot is unoccupied.
 */
//class StateAdapter implements {
//}


/*
{
    "race": {
        "background": {
            "y1": float,
            "y2": float,
            "xPosition": float
        }
    }
}
 */